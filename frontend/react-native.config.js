module.exports = {
  // No generice, extra platform-specific configuration is needed for this project
    project: {
      ios: {},
      android: {},
    },
    // Extra assets to be included in the compilation
    assets: ['./assets/fonts'],
};